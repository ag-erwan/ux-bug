<div{{ attributes }}>
    {{ form_start(form) }}
        {{ form_row(form.champ1) }}
        {% if form.champ2 is defined %}
            {{ form_row(form.champ2) }}
        {% endif %}
    {{ form_end(form) }}
</div>
